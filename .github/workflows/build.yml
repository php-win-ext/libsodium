name: build
on: [push]
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - uses: microsoft/setup-msbuild@v1.0.2
      - run: '& $env:GITHUB_ACTION_PATH/build.ps1'
        shell: pwsh
